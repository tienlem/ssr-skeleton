/*
 * Login Messages
 *
 * This contains all the text for the AppLayout container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppLayout';

export default defineMessages({
  logout: {
    id: `${scope}.logout`,
    defaultMessage: 'Logout',
  },
});
